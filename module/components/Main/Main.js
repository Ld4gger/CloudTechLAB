export default function Main({ children }) {
    return (
        <div
        style={{
            background: "#ffffff",
            alignItems: "center",
            marginLeft: "auto",
            marginRight: "auto",
            fontSize: 45,
          }}>{children}</div>
    );
}
